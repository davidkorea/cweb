<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <a class="navbar-brand" href="/">C-Web</a>
        </div>

        <div class="collapse navbar-collapse">

           <ul class="nav navbar-nav">
             <li class="active">
               <%= link_to("Home", ccases_path) %>
             </li>
           </ul>

           <ul class="nav navbar-nav">
             <li class="active">
               <%= link_to("Created Case", ccases_path) %>
             </li>
           </ul>

           <ul class="nav navbar-nav">
             <li class="active">
               <%= link_to("Accepted Case", ccases_path) %>
             </li>
           </ul>

            <ul class="nav navbar-nav navbar-right">

                <li>
                <% if !current_user %>
                  <li><%= link_to("注册", new_user_registration_path) %> </li>
                  <li><%= link_to("登录", new_user_session_path) %></li>
                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Hi!, <img src= "<%= render_user_avatar(current_user, 20) %>"><%= current_user.display_name %>
                        <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                      <% if current_user.admin? %>
                      <li> <%= link_to("Admin Panel", admin_ccases_path) %> </li>
                      <li> <%= link_to("User Management", admin_users_path) %> </li>
                      <% end %>
                      <li><%= link_to "个人中心", edit_account_user_path(current_user) %></li>

                        <li> <%= link_to("退出", destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>

                </li>
            </ul>
        </div>
    </div>
</nav>
<!-- 以上部分，不需要记忆 -->
